package battle.entity.characters;

public class Player extends PartyMembers {

    protected Player(String name, int maxHP, int maxSP, int attack, int defense, int speed) {
        super(name, maxHP, maxSP, attack, defense, speed);
    }
    
}
